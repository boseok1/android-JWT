server:
  port: 8001
  servlet:
    context-path: /
    
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shareHouse?serverTimezone=Asia/Seoul
    username: shareHouse
    password: bitc5600
    driver-class-name: com.mysql.cj.jdbc.Driver 
    
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true